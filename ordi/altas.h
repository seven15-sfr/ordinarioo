#ifndef ALTAS_H
#define ALTAS_H

#include <stdio.h>
#include <stdlib.h>
#include "struct.h"

int Altas(struct Persona **prtFrente, struct Persona **ptrFinal);
struct Persona* nuevaPersona(void);
struct Alumno*  nuevoAlumno(void);


#endif
